const FileSystem = () => (
    <div>
      <h1>File System</h1>
      <p>Node.js allows you to work with the file system through the `fs` module...</p>
    </div>
  );
  
  export default FileSystem;
  