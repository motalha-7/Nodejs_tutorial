const Npm = () => (
    <div>
      <h1>Node Package Manager (NPM)</h1>
      <p>NPM is the default package manager for Node.js. It allows you to install packages...</p>
    </div>
  );
  
  export default Npm;
  