const Debugging = () => (
    <div>
      <h1>Debugging Node.js Applications</h1>
      <p>Debugging in Node.js can be done using the `debugger` statement and tools like `node --inspect`...</p>
    </div>
  );
  
  export default Debugging;
  